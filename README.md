# 作品集部署包

此目录已经整理为 GitHub Pages 可识别的静态站点：

- `index.html`：网站入口文件
- `.nojekyll`：跳过不需要的 Jekyll 构建

部署时把本目录中的文件上传到仓库根目录，然后在仓库 **Settings → Pages** 中选择：

1. **Source**：Deploy from a branch
2. **Branch**：main
3. **Folder**：/ (root)

保存后等待 GitHub Pages 发布即可。
